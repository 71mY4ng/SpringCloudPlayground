spring:
  application:
    name: play-public-api
  profiles:
    active: prod #'@activatedProperties@' #dev

server:
  port: 8020

management:
  endpoint:
    metrics:
      enabled: true
    prometheus:
      enabled: true
